# גלריה מבוססת מסד נתונים — מקומי

טבלת SQLite בשם `samples` מחזיקה את התצפיות והסרטונים, עם מפתח זר אל `observations_species`. המודל הקודם Observation הוסב ל-Sample תוך שמירת הרשומות. כתובות הקהילה נשארו `/observations/`.

ניהול: http://127.0.0.1:8000/admin/observations/sample/

הגלריה קוראת את `/catalog.js` שנוצר כעת מתוך המסד בכל בקשה. קובץ `dist/catalog.js` נשמר כמקור היסטורי לייבוא בלבד; עריכתו אינה משנה את הגלריה. אזורי הסינון נוצרים מתוך הנתונים. רק דגימות מפורסמות, שלא נמחקו, ושהושלמו בהן מין, מדינה, אזור ושנה, מוצגות בגלריה.

מקור ההשלמה: האקסל של המשתמש, לשונית Observations, מסע V בשורה 18 — Boaz Liebes, Romblon, Philippines, ינואר 2026, Indo Pacific. יום התחלת מסע אינו יום התצפית ולכן אינו מועתק. התאמה יחידה לפי שם מין וקוד מסע בלשונית samples נשמרת במידע המקור; אין התאמה מקורבת אוטומטית. שמות וקישורי YouTube נלקחו מהגלריה הקיימת. האקסל לא שונה.

ייבוא ראשון: 107 סרטונים, 103 מפורסמים. ארבע רשומות ממתינות: שני סרטוני אילת ללא שנת תצפית באקסל, וסרטוני הסיכום של רומבלון ואכזיב ללא מין יחיד. מידע המקור והחוסרים מופיעים בטופס הניהול. מין של סרטון סיכום אינו מנוחש.

כלי הייבוא `import_gallery_samples` מקבל חילוץ JSON לקריאה בלבד מהאקסל, ודורש `--owner`. ללא `--apply` מוצגת ספירה בלבד. מזהה `youtube:<id>` מונע ייבוא כפול בהרצה חוזרת; רשומות קיימות אינן נדרסות. גיבוי SQLite ודוח נוצרים ב-backups. השינויים והנתונים מקומיים בלבד; פריסת קוד אינה מעבירה את תוכן המסד ל-Render.
